import type { Metadata } from 'next'
import Link from 'next/link'
import { getComments,getPostBySlug } from '@/lib/db'
import { markdownToHtml } from '@/lib/markdown'
const SITE='https://blog.suhanurrahman.com'
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=await getPostBySlug(slug);if(!p)return{title:'Not Found'};const title=p.seoTitle||p.title;const description=p.seoDescription||p.excerpt||`${p.title} by Suhanur Rahman.`;const canonical=p.canonicalUrl||`${SITE}/${p.slug}/`;return{title,description,alternates:{canonical},robots:p.noindex?{index:false,follow:false}:undefined,keywords:p.seoKeywords||undefined,openGraph:{type:'article',url:canonical,title,description,images:(p.ogImage||p.coverImage)?[{url:p.ogImage||p.coverImage!}]:undefined,publishedTime:p.publishedAt||undefined,modifiedTime:p.updatedAt}}}
function formatDate(v:string|null){return v?new Intl.DateTimeFormat('en',{dateStyle:'long'}).format(new Date(v)):''}
export default async function PostPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=await getPostBySlug(slug);if(!p)return <main className="container article-shell"><h1 className="serif">Article not found</h1><Link href="/">Back to journal</Link></main>;const comments=await getComments(p.id);const html=p.contentFormat==='HTML'?p.content:markdownToHtml(p.content);const jsonLd={'@context':'https://schema.org','@type':'BlogPosting',headline:p.title,description:p.seoDescription||p.excerpt||undefined,datePublished:p.publishedAt||undefined,dateModified:p.updatedAt,mainEntityOfPage:`${SITE}/${p.slug}/`,author:{'@type':'Person',name:'Suhanur Rahman',url:'https://suhanurrahman.com/'},image:(p.ogImage||p.coverImage)?[p.ogImage||p.coverImage!]:undefined,articleSection:p.categoryName||undefined};return <main><header className="header"><div className="container header-inner"><Link className="brand" href="/">SUHANUR RAHMAN / JOURNAL</Link><nav className="nav"><Link href="/">Journal</Link><a href="https://suhanurrahman.com/">Portfolio ↗</a></nav></div></header><div className="container article-shell"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><article><header className="article-head"><Link className="meta" href={p.categorySlug?`/category/${p.categorySlug}/`:'/'}>{p.categoryName||'Journal'}</Link><h1>{p.title}</h1><p className="article-excerpt">{p.excerpt||''}</p><div className="article-meta"><span>{formatDate(p.publishedAt)}</span><span>·</span><span>By Suhanur Rahman</span></div></header>{p.coverImage?<img className="article-cover" src={p.coverImage} alt={p.title}/>:null}<div className="prose" dangerouslySetInnerHTML={{__html:html}}/><section className="comments"><h2 className="serif">Comments</h2>{comments.map(c=><div className="comment" key={c.id}><strong>{c.name}</strong><p>{c.body}</p></div>)}<form className="comment-form" action="/api/comments" method="post"><input type="hidden" name="postId" value={p.id}/><input name="name" placeholder="Your name" required/><input name="email" type="email" placeholder="Email (optional)"/><textarea name="body" placeholder="Write a comment…" required/><button className="button" type="submit">Submit comment</button><small className="muted">Comments are held for moderation before appearing publicly.</small></form></section></article></div><footer className="footer"><div className="container">© 2026 Suhanur Rahman · Journal</div></footer></main>}
